name: Publish Packages

on:
  release:
    types: [published]
  repository_dispatch:
    types: [trigger-publish]
  workflow_dispatch:

# Unified Node.js version for stability

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - name: Generate Token
        id: app-token
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          owner: citadel-app

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm install --legacy-peer-deps --no-package-lock
        env:
          CITADEL_RELEASE_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Core
        run: npm run build
        working-directory: packages/core

      - name: Publish Core
        run: npm publish
        working-directory: packages/core
        env:
          CITADEL_RELEASE_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build UI
        run: npm run build
        working-directory: packages/ui

      - name: Publish UI
        run: npm publish
        working-directory: packages/ui
        env:
          CITADEL_RELEASE_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build App
        run: npm run build
        env:
          MAIN_VITE_GITHUB_CLIENT_ID: ${{ secrets.CITADEL_GITHUB_CLIENT_ID }}

      - name: Publish App
        run: npm publish
        env:
          CITADEL_RELEASE_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Trigger Desktop Build
        run: |
          curl -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ steps.app-token.outputs.token }}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/citadel-app/citadel-desktop/dispatches \
            -d '{"event_type": "new-core-release"}'
